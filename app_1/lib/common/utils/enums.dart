enum QueryType { all, popular, modern, classic, minimalist }

enum WishlistToggle {
  retrieve,
  delete,
  add,
}

enum FetchOrdersTypes {
  pending,
  delivered,
  cancelled,
}

enum ReloadCount {
  delete,
  reload,
  update,
}

/*enum NotificationCount {
  update,
  reload,
}*/
